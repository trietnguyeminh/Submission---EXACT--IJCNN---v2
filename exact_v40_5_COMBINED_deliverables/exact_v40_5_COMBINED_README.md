# EXACT v40.5 COMBINED deliverable

Use `exact_v40_5_one_run_COMBINED.ipynb` as the single primary file.

It embeds the pipeline previously stored in `exact_v40_5_one_run_pipeline.py`; no external `.py` is required.

Run top-to-bottom once. The output folder will contain JSON stages including `stage_00_combination_map.json`, which documents exactly what was merged.
